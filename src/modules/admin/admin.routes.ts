import { Router } from "express";

// TODO(next phase): staff invite/edit/remove and role assignment, matching
// the admin Users & Roles view already built in the frontend.
export const adminRouter = Router();
